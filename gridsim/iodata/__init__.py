"""
The input/output module provides loader and saver interfaces.

Currently Gridsim provides loader for CSV file (:class:`.CSVReader`) and savers
for CSV file (:class:`.CSVSaver`) and images -- .png, .jpg, .pdf, etc. --
(:class:`.FigureSaver`).

"""